# Decision Log

This document records structural decisions for `living-aegis-prototype`.

## Initial Repository Structure

- The main game repository and prototype repository are separated.
- The simulator repository is also managed separately.
- The root page acts as a prototype launcher, not a game screen.
- Each prototype is managed as an independently runnable folder.
- The project starts with plain HTML, CSS, and JavaScript without build tools.
